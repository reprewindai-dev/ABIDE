interface:
  display_name: "ABIDE"
  short_description: "Repo-specific patterns and workflows for ABIDE"
  default_prompt: "Use the ABIDE repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true